# Archived Next.js Prototype

This folder contains the earlier Next.js proof of concept. It is kept only as a reference while the current MVP lives in `../blazor-ui`.

For current development and review, use:

```bash
bash ./start-all.sh
```

Then open `http://127.0.0.1:5050`.
